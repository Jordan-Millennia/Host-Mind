{
  "name": "roomos",
  "version": "0.1.0",
  "private": true,
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "pnpm --filter @roomos/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "db:generate": "pnpm --filter @roomos/db exec prisma generate",
    "db:migrate": "pnpm --filter @roomos/db exec prisma migrate dev",
    "db:migrate:deploy": "pnpm --filter @roomos/db exec prisma migrate deploy",
    "db:seed": "pnpm --filter @roomos/db exec prisma db seed",
    "db:studio": "pnpm --filter @roomos/db exec prisma studio",
    "worker:dev": "pnpm --filter @roomos/worker dev",
    "worker:build": "pnpm --filter @roomos/worker build",
    "worker:scheduler": "pnpm --filter @roomos/worker dev scheduler"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@10.0.0"
}
