{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable pnpm && cd roomos && pnpm install --frozen-lockfile && pnpm db:generate && pnpm --filter @roomos/web build"
  },
  "deploy": {
    "startCommand": "cd roomos && pnpm db:migrate:deploy && pnpm --filter @roomos/web start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
